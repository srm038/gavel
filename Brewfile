brew "pandoc"
cask "font-times-new-roman"

# TeX distribution for pandoc PDF output (xelatex).
# Full TeX Live is ~6 GB; use TinyTeX instead:
#   brew install --cask mactex   # alternative full install (macOS)
#   ./scripts/install-tex.sh             # recommended — installs TinyTeX (~200 MB)
